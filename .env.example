# 笔记仓库同步的配置示例。
#
# 用法：复制成 .env.local 再改（.env.local 已被 .gitignore 忽略，不会提交）。
# 同步脚本（scripts/sync-notes.mjs）和 Next 都会读这两个文件，
# 优先级：命令行/CI 环境变量 > .env.local > .env。

# 笔记仓库的 Git 地址。私有仓库走 HTTPS + SKBLOG_NOTES_TOKEN，
# 或让 CI 用 SSH（git@github.com:you/vault.git）。
SKBLOG_NOTES_REPO=https://github.com/you/notes.git

# 只拉这个分支；不写就用仓库的默认分支。
# SKBLOG_NOTES_BRANCH=main

# HTTPS 私有仓库的访问令牌（GitHub 上建议用 Fine-grained token，只给 Contents: Read；
# Gitee 用「设置 → 私人令牌」，注意它没有只读粒度，别复用别处用过的令牌）。
# SKBLOG_NOTES_TOKEN=

# 令牌对应的用户名。GitHub / GitCode / GitLab 可以留空（走各平台的惯例），
# Gitee 必须填真实账号名 —— 它的私有仓库 clone 要的就是「账号 + 令牌」。
# SKBLOG_NOTES_USER=your-gitee-name

# 直接用本地目录当笔记仓库：设了就完全不联网、不克隆（本地开发推荐）。
# 相对路径按项目根解析。
# SKBLOG_NOTES_DIR=../notes-vault

# 缓存目录，默认 .notes（已 gitignore）。
# SKBLOG_NOTES_CHECKOUT=.notes

# 只扫描笔记仓库里的某个子目录，例如 Blog；不写就扫整个仓库。
# SKBLOG_NOTES_SUBDIR=Blog

# 设为 1 时跳过同步，沿用已有缓存。
# SKBLOG_SKIP_NOTES_SYNC=1

# 设为 1 时同步失败直接报错退出；CI（含 Vercel）默认就是严格模式。
# SKBLOG_NOTES_STRICT=1
